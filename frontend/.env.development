    # Development environment variables
    # Prefix with VITE_ to expose them to the client-side code

    # Default backend URL — lab provisioner overrides this per user via VITE_API_BASE_URL
    VITE_API_BASE_URL=http://localhost:5001/api

    # Add other development-specific variables from your template here:
    # VITE_WEBSITE_NAME=MyApp (Dev)
    # VITE_FIREBASE_API_KEY=...
    # VITE_AUTH0_CLIENT_ID=...
    # VITE_COGNITO_USER_POOL_ID=...
    # ... etc.